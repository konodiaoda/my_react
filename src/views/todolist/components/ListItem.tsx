import {IListItemProps} from "@/views/todolist/interface/Task";

const ListItem = ({taskId,content, isDone}:IListItemProps) => {

	const handleDeleteTask = () => {

	}

	const handleSelectTask = () => {
	}

	return(
		<div>

		</div>
	)
}

export  default  ListItem;