import { useRoutes } from 'react-router-dom';

// routes


// ==============================|| ROUTING RENDER ||============================== //

 const ThemeRoutes = ()=> {
	return useRoutes([]);
}
export default ThemeRoutes;
